version: '2'
services:
  graphql-engine:
    image: hasura/graphql-engine:latest
    ports:
      - '80:8080'
    restart: always
    environment:
      HASURA_GRAPHQL_DATABASE_URL: postgres://hasura:HasuraGezi123@geziapp-test.ccgvrluzqu3o.us-east-2.rds.amazonaws.com:5432/gezi_db
      HASURA_GRAPHQL_ACCESS_KEY: HasuraGezi@123
    command:
      - graphql-engine
      - serve
      - --enable-console
